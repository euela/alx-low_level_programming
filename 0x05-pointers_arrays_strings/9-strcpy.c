#include "main.h"

/**
 *  * prints elements of an array of integers
 *   *
 *    *Return: Pointer
 *     */

char *_strcpy(char *dest, char *src)
{
	int i, count;

		for (count = 0; src[count] != '\0'; count++)
				{
						}

			for (i = 0; i <= count ; i++)
					{
								dest[i] = src[i];
									}
				return (dest);
}
