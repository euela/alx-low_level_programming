#include <main.h>

/*display output 10 times*/

void print_alphabet_x10(void)
{
		for (int j = 0;j < 10;j++)
				{
							for (int i = 'a';i <= 'z';i++)
										{
													putchar(i);		
															}
									putchar(10);
										}
}
